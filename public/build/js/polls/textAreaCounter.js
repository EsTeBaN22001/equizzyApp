const charCounter=document.querySelector("span.char-counter"),textarea=document.querySelector("textarea.char-to-count");let count=0;charCounter.textContent=count,textarea.addEventListener("input",(function(t){const e=textarea.value.length;count=e,charCounter.textContent=count,count<0||count>500?(charCounter.classList.add("error"),count=500):charCounter.classList.remove("error")}));